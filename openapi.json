{
  "openapi": "3.0.0",
  "info": {
    "title": "MCP Config Auto-Generator",
    "version": "1.0.0"
  },
  "paths": {
    "/generate_config": {
      "post": {
        "summary": "Generate MCP config",
        "description": "API endpoint for generating MCP config based on Swagger/OpenAPI spec.",
        "operationId": "generateConfig",
        "requestBody": { 
          "content": {
            "application/json": {
              "schema": { $ref: "#/components/schemas/RequestBodySchema" } 
            }
          }
        },
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": { $ref: "#/components/schemas/ConfigResponseSchema" } 
              }
            }
          },
          "401": {
            "description": "Unauthorized"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "RequestBodySchema": {
        "type": "object",
        "properties": {
          // Define properties for your OpenAPI spec input here. 
        }
      },
      "ConfigResponseSchema": {
        "type": "object",
        "properties": {
          // Define properties for the MCP config output here. 
        }
      }
    }
  }
}